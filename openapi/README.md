# OpenAPI snapshot for StockGuard API

Source of truth at runtime: `GET http://localhost:5087/swagger/v1/swagger.json`

Refresh from a running API:

```bash
npm run generate:api-types:refresh
```

Offline / CI generation uses this file via `npm run generate:api-types`.
